@layouts.app()
@formProd({action: route('products.store')})
    <div>
        <div>
            <label > email: </label>
            @!input({ name:'email', type:'text'})
        </div>
        <div>
            <label > Senha </label>
            @!input({name:'password', type:'password',})
        </div>

        @button({type:"submit"})
        Enviar
        @end
    </div>
@end
@end